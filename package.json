{
  "scripts": {
    "build:dev": "eleventy --serve --input=./src --output=./docs",
    "build:prod": "eleventy --input=./src --output=./docs"
  },
  "devDependencies": {
    "@11ty/eleventy": "^1.0.1"
  }
}
